#
# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

# Default values for HugeGraph HStore Helm chart.
# See values-single.yaml and values-cluster.yaml for presets.

fullnameOverride: ""
nameOverride: ""

imagePullSecrets: []

pd:
  replicas: 3
  image:
    repository: hugegraph/pd
    tag: helm-dev
    pullPolicy: IfNotPresent
  # Empty preserves the image entrypoint's automatic JVM sizing. The chart
  # renders its partition settings below as -D system properties ahead of
  # this value, so flags placed here win on conflict.
  javaOpts: ""
  # Partition sharding, rendered as -D system properties that outrank the
  # image's conf/application.yml. PD copies both values into its own
  # persisted metadata at first bootstrap only; from then on the stored
  # values are authoritative, so changing these later (or scaling
  # store.replicas across the derivation boundary) has no effect on an
  # initialized cluster. Post-bootstrap changes go through PD's config API
  # and take effect on existing shard groups only when a partition patrol
  # is triggered; see the README's Partition Sharding section.
  partition:
    # Shard replicas per partition, seeding PD at first bootstrap only.
    # Empty derives 3 when store.replicas is at least 3, else 1, so a fresh
    # multi-store install gets store-level HA instead of the image default
    # of 1. The derivation skips 2 because PD clamps a shard count of 2 to
    # 1 (two shards cannot elect a leader). An explicit value must be odd
    # and must not exceed store.replicas.
    defaultShardCount: ""
    # Maximum shards per store, seeding PD at first bootstrap only. Also
    # fixes the initial partition count: store.replicas x this value /
    # shard count. The derived shard count of 3 gives the default topology
    # 12 partitions instead of 36; raise this value to compensate when more
    # partitions are wanted. Empty preserves the image default of 12.
    storeMaxShardCount: ""
  ports:
    grpc: 8686
    rest: 8620
    raft: 8610
  dataPath: /hugegraph-pd/pd_data
  storage:
    size: 10Gi
    storageClassName: ""
  resources: {}
  # Pod-level and container-level securityContext. Empty by default because the
  # published images run as root; set these to satisfy a restricted namespace.
  podSecurityContext: {}
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop: ["ALL"]
    seccompProfile:
      type: RuntimeDefault
  # required | preferred | disabled. "preferred" schedules on clusters with
  # fewer nodes than replicas; production should use "required" (see
  # values-cluster.yaml) so one node failure cannot take out PD quorum.
  antiAffinity: preferred
  # Scheduling. `affinity` takes precedence over the antiAffinity preset.
  nodeSelector: {}
  tolerations: []
  affinity: {}
  topologySpreadConstraints: []
  priorityClassName: ""
  podAnnotations: {}
  podLabels: {}
  # Extra environment variables appended to the pd container.
  extraEnv: []
  # Gives a JVM with an on-disk store time to shut down cleanly on drain.
  terminationGracePeriodSeconds: 300
  serviceAccount:
    create: true
    name: ""
    annotations: {}
    # This chart makes no Kubernetes API calls, so no token is mounted.
    automountServiceAccountToken: false
  pdb:
    enabled: true
    minAvailable: 2
  # Startup can take a while during Raft bootstrap
  probes:
    startup:
      failureThreshold: 30
      periodSeconds: 10
      timeoutSeconds: 5
    readiness:
      periodSeconds: 10
      failureThreshold: 3
      timeoutSeconds: 5
    liveness:
      periodSeconds: 20
      failureThreshold: 3
      timeoutSeconds: 5

store:
  replicas: 3
  image:
    repository: hugegraph/store
    tag: helm-dev
    pullPolicy: IfNotPresent
  # Empty preserves the image entrypoint's automatic JVM sizing.
  javaOpts: ""
  ports:
    grpc: 8500
    raft: 8510
    rest: 8520
  dataPath: /hugegraph-store/storage
  storage:
    size: 50Gi
    storageClassName: ""
  resources: {}
  podSecurityContext: {}
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop: ["ALL"]
    seccompProfile:
      type: RuntimeDefault
  # required | preferred | disabled. "preferred" schedules on clusters with
  # fewer nodes than replicas; production should use "required" (see
  # values-cluster.yaml) so one node failure cannot co-locate shard replicas.
  antiAffinity: preferred
  # Scheduling. `affinity` takes precedence over the antiAffinity preset.
  nodeSelector: {}
  tolerations: []
  affinity: {}
  topologySpreadConstraints: []
  priorityClassName: ""
  podAnnotations: {}
  podLabels: {}
  # Extra environment variables appended to the store container.
  extraEnv: []
  # Gives a JVM with an on-disk store time to shut down cleanly on drain.
  terminationGracePeriodSeconds: 300
  serviceAccount:
    create: true
    name: ""
    annotations: {}
    # This chart makes no Kubernetes API calls, so no token is mounted.
    automountServiceAccountToken: false
  pdb:
    enabled: true
    minAvailable: 2
  waitImage: curlimages/curl:8.5.0
  # Bound the PD-quorum wait so a cluster that never reaches quorum fails
  # visibly instead of sitting in Init:0/1 forever.
  waitTimeoutSeconds: 900
  # Optional bounds for the PD-quorum wait init container.
  waitResources: {}
  probes:
    startup:
      failureThreshold: 40
      periodSeconds: 10
      timeoutSeconds: 5
    readiness:
      periodSeconds: 10
      failureThreshold: 3
      timeoutSeconds: 5
    liveness:
      periodSeconds: 20
      failureThreshold: 3
      timeoutSeconds: 5

server:
  replicas: 3
  image:
    repository: hugegraph/server
    tag: helm-dev
    pullPolicy: IfNotPresent
  # Empty preserves the image entrypoint's automatic JVM sizing.
  javaOpts: ""
  port: 8080
  backend: hstore
  resources: {}
  # Server is stateless and may scale past the node count via HPA, so the
  # default only prefers spreading. Use "required" when replicas are always
  # fewer than schedulable nodes.
  # required | preferred | disabled
  antiAffinity: preferred
  # Scheduling. `affinity` takes precedence over the antiAffinity preset.
  nodeSelector: {}
  tolerations: []
  affinity: {}
  topologySpreadConstraints: []
  priorityClassName: ""
  podAnnotations: {}
  podLabels: {}
  # Extra environment variables appended to the server container.
  extraEnv: []
  # Allows in-flight requests to drain before the Server is stopped.
  terminationGracePeriodSeconds: 60
  serviceAccount:
    create: true
    name: ""
    annotations: {}
    # This chart makes no Kubernetes API calls, so no token is mounted.
    automountServiceAccountToken: false
  podSecurityContext: {}
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop: ["ALL"]
    seccompProfile:
      type: RuntimeDefault
  # Server has no quorum to preserve, so no PodDisruptionBudget by default.
  pdb:
    enabled: false
    minAvailable: 2
  # Image used by the Helm test hook.
  waitImage: curlimages/curl:8.5.0
  # Optional resources for the Helm test hook container.
  testResources: {}
  restServer:
    # Empty preserves the image's restserver.min_free_memory default.
    minFreeMemory: ""
    # Empty preserves the image's batch.max_write_threads default.
    batchMaxWriteThreads: ""
  # Distributed HStore: the init-store gate must be explicitly false, so that
  # concurrent Server replicas never initialize the same backend. No
  # HG_SERVER_SKIP_INIT and no init Job.
  initStoreEnabled: false
  auth:
    enabled: true
    autoGenerateSecret: true
    # Secret must contain key "password" (no newlines). Applied as
    # auth.admin_pa when the admin account is first created; changing the
    # Secret later does not rotate an existing cluster's password. When empty,
    # the chart creates a kept release-admin Secret.
    existingSecret: ""
    # JWT signing key for auth.token_secret / HG_SERVER_AUTH_TOKEN_SECRET.
    # Must be identical on every Server replica or Hubble login fails behind
    # the Service. Empty existingSecret: chart creates a kept Secret (lookup
    # keeps the value across upgrades). BYO with existingSecret + key.
    tokenSecret:
      existingSecret: ""
      key: token_secret
  service:
    type: ClusterIP
    annotations: {}
  ingress:
    enabled: false
    className: ""
    annotations: {}
    hosts:
      - host: hugegraph.local
        paths:
          - path: /
            pathType: Prefix
    tls: []
  hpa:
    enabled: false
    minReplicas: 3
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
  probes:
    startup:
      # 450s covers the 300s storage wait and Server process startup.
      failureThreshold: 90
      periodSeconds: 5
      timeoutSeconds: 5
    readiness:
      periodSeconds: 10
      failureThreshold: 3
      timeoutSeconds: 5
    liveness:
      periodSeconds: 20
      failureThreshold: 3
      timeoutSeconds: 5

# Hubble UI is enabled by default. It serves plain HTTP; keep it on a ClusterIP
# Service or behind an HTTPS-terminating Ingress, never directly on an untrusted
# network.
hubble:
  enabled: true
  # pd: discover the cluster through PD (enables the cluster operations view).
  # direct: talk to the Server client Service only, without PD discovery.
  # Current Hubble images require server.auth to be enabled: the UI login
  # authenticates against the cluster, so rendering fails otherwise unless
  # allowWithoutServerAuth explicitly overrides for images that support it.
  mode: pd
  allowWithoutServerAuth: false
  image:
    repository: hugegraph/hubble
    # The draft tracks latest until the next HugeGraph release tag is available.
    # Pin the release tag and switch to IfNotPresent before stable publication.
    tag: latest
    pullPolicy: Always
  port: 8088
  # Hubble keeps UI connection metadata, including any graph credentials
  # entered in the UI, in an embedded per-instance H2 database, so the
  # Deployment is fixed at a single replica (pointing SPRING_DATASOURCE_URL
  # at an external database via extraEnv is not a supported configuration).
  # Without persistence that metadata is lost on Pod replacement; graph data
  # is unaffected. size and storageClassName apply at install time only, and
  # the PVC is kept on helm uninstall.
  persistence:
    enabled: false
    size: 1Gi
    storageClassName: ""
  resources: {}
  # When persistence is enabled and the pod runs as non-root, set a matching
  # fsGroup here so H2 can write /hubble-data.
  podSecurityContext: {}
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop: ["ALL"]
    seccompProfile:
      type: RuntimeDefault
  nodeSelector: {}
  tolerations: []
  affinity: {}
  topologySpreadConstraints: []
  priorityClassName: ""
  podAnnotations: {}
  podLabels: {}
  # Extra environment variables appended to the hubble container.
  extraEnv: []
  terminationGracePeriodSeconds: 30
  serviceAccount:
    create: true
    name: ""
    annotations: {}
    # This chart makes no Kubernetes API calls, so no token is mounted.
    automountServiceAccountToken: false
  service:
    type: ClusterIP
    annotations: {}
  ingress:
    enabled: false
    className: ""
    annotations: {}
    hosts:
      - host: hubble.local
        paths:
          - path: /
            pathType: Prefix
    tls: []
    # Hubble serves plain HTTP, so an Ingress without tls is rejected at
    # render time unless this is explicitly set to true for a trusted
    # network.
    allowPlainHttp: false
  probes:
    startup:
      failureThreshold: 30
      periodSeconds: 5
      timeoutSeconds: 5
    readiness:
      periodSeconds: 10
      failureThreshold: 3
      timeoutSeconds: 5
    liveness:
      periodSeconds: 20
      failureThreshold: 3
      timeoutSeconds: 5

# No init Job; see README.md for the HStore initialization contract.
# Install with: helm install ... --wait   (no --wait-for-jobs)
